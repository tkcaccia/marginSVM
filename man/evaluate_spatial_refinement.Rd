% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarking.R
\name{evaluate_spatial_refinement}
\alias{evaluate_spatial_refinement}
\title{Evaluate a spatial label refinement}
\usage{
evaluate_spatial_refinement(
  truth,
  initial,
  refined,
  boundary = NULL,
  regions = NULL,
  sparse = NULL,
  elapsed = NA_real_,
  method = NULL
)
}
\arguments{
\item{truth}{Reference assignments.}

\item{initial}{Initial noisy assignments.}

\item{refined}{Assignments returned by a refinement method.}

\item{boundary}{Optional logical vector marking boundary observations.}

\item{regions}{Optional region identifier. The least and most frequent
regions define sparse- and dense-region accuracy when `sparse` is omitted.}

\item{sparse}{Optional logical vector marking sparse-region observations.}

\item{elapsed}{Optional elapsed runtime in seconds.}

\item{method}{Optional method name included in the returned row.}
}
\value{
A one-row data frame containing the evaluation measures.
}
\description{
Computes primary and failure-mode metrics for label repair. Returned measures
include recovery (accuracy and adjusted Rand index), class imbalance robustness
(macro and worst recall), boundary/sparse reliability, and damage.
}
\details{
`correction_recall` is the fraction of initially incorrect labels that are
repaired. `damage_rate` is the fraction of initially correct labels that are
flipped to an incorrect label. `changed_precision` is the fraction of
modified sites that are correct after refinement. Empty strata are reported as
`NA` rather than imputed as zero. Adjusted Rand index is calculated directly
from the vectors and does not require an external package.
}
\examples{
truth <- factor(c("A", "A", "B", "B"))
initial <- factor(c("A", "B", "B", "A"))
refined <- truth
evaluate_spatial_refinement(truth, initial, refined)
}
