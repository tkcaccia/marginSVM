% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradient_regions.R
\name{simulate_gradient_regions}
\alias{simulate_gradient_regions}
\title{Simulate layered gradient-mixture tissue regions}
\usage{
simulate_gradient_regions(
  n = 50000L,
  minority = 0.05,
  dimensions = 2L,
  samples = 1L,
  seed = 1L,
  curvature = 0.04,
  density_profile = c("uniform", "moderate", "strong", "extreme")
)
}
\arguments{
\item{n}{Total number of observations across all tissues.}

\item{minority}{Fraction of minority labels in every area.}

\item{dimensions}{Either 2 or 3 spatial dimensions.}

\item{samples}{Number of independent tissues.}

\item{seed}{Random seed.}

\item{curvature}{Boundary waviness on the unit coordinate scale.}

\item{density_profile}{Relative observation concentration across the four
tissue areas. Use `"uniform"`, `"moderate"`, `"strong"`, `"extreme"`,
or a positive numeric vector of length four.}
}
\value{
A `spatial_refinement_benchmark` containing `xy`, mixed `labels`,
  reference `truth`, `samples`, `area`, boundary indicators, and
  sparse-region indicators.
}
\description{
Generates four ordered tissue areas whose reference classes are `A`, `B`,
`B`, and `C`. Within the areas, a configurable minority fraction is labeled
`B`, `A`, `C`, and `B`, respectively. Independent tissues are generated with
separate curved boundaries and coordinate offsets.
}
\examples{
sim <- simulate_gradient_regions(n = 4000, minority = 0.05, samples = 2)
table(sim$area, sim$labels)
}
